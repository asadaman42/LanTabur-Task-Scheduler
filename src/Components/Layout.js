import React from 'react';
import Navbar from './Shared/Navbar';

const Layout = () => {
    return (
        <div>
            <Navbar></Navbar>
            
        </div>
    );
};

export default Layout;